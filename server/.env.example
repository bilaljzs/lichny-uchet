PORT=4000
JWT_SECRET=change-me-to-a-long-random-string
# Postgres connection string — e.g. from Supabase: Project Settings → Database → Connection string (URI)
DATABASE_URL=postgres://user:password@host:5432/dbname
